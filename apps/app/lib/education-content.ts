/**
 * The education catalog moved to @finnacalc/shared so the marketing site's
 * education pages and the app's read the same source. Re-exported here because
 * the app imports it from "@/lib/education-content" in several places.
 */
export * from "@finnacalc/shared/education-content"
