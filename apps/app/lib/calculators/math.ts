/**
 * Calculator math now lives in @finnacalc/shared: the landing page's live
 * widget and the app's calculator screens must compute identically, and one
 * copy is the only way to guarantee that.
 */
export * from "@finnacalc/shared/calculators"
